{
  "name": "ai-red-mahjong",
  "version": "1.0.0",
  "description": "可在浏览器游玩的四人红中麻将",
  "private": true,
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/chenrang418-glitch/ai-red-mahjong.git"
  },
  "type": "module",
  "scripts": {
    "dev": "vite",
    "dev:online": "vite build --config server/vite.config.ts --configLoader runner && wrangler d1 migrations apply DB --local --config server/wrangler.jsonc && wrangler dev --config server/wrangler.jsonc --local --port 8787",
    "build": "npm run typecheck && npm run build:frontend",
    "build:frontend": "vite build --configLoader runner",
    "build:worker": "vite build --config server/vite.config.ts --configLoader runner",
    "build:online": "npm run build:worker && npm run dry-run:online",
    "dry-run:online": "wrangler deploy --dry-run --config server/wrangler.jsonc",
    "typecheck:online": "tsc -p server/tsconfig.json",
    "db:migrate:local": "wrangler d1 migrations apply DB --local --config server/wrangler.jsonc",
    "db:migrate:remote": "wrangler d1 migrations apply DB --remote --config server/wrangler.jsonc",
    "deploy:online": "npm run typecheck:online && vite build --config server/vite.config.ts --configLoader runner && npm run db:migrate:remote && wrangler deploy --config server/wrangler.jsonc",
    "start": "node server.mjs",
    "preview": "vite preview",
    "test": "vitest --configLoader runner",
    "test:run": "vitest run --configLoader runner",
    "test:sanguosha": "vitest run sanguosha --configLoader runner",
    "sanguosha:soak": "vite-node scripts/sanguosha-soak.mjs --",
    "sanguosha:huashen-audit": "vite-node scripts/huashen-audit.mjs",
    "test:e2e": "playwright test",
    "test:online": "npm run test:online:smoke",
    "test:online:smoke": "npm run build:worker && npm run test:online:prepared",
    "test:online:prepared": "node scripts/online-smoke.mjs",
    "typecheck": "vue-tsc --noEmit",
    "sanguosha:duorui-audit": "vite-node scripts/sanguosha-duorui-audit.mjs"
  },
  "dependencies": {
    "vue": "^3.5.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260815.1",
    "@playwright/test": "^1.62.1",
    "@vitejs/plugin-vue": "^5.0.0",
    "miniflare": "5.20260811.1-alpha",
    "typescript": "^5.0.0",
    "vite": "^6.0.0",
    "vitest": "^3.0.0",
    "vue-tsc": "^2.0.0",
    "wrangler": "^4.123.0"
  }
}
