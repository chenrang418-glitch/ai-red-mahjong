# 管理接口密钥。不配置的话 /api/admin/* 整体返回 404，等于关闭管理功能。
ADMIN_TOKEN=换成一段足够长的随机字符串

# 监听设置。用 Nginx 反代时绑 127.0.0.1，不要直接暴露到公网。
PORT=8787
HOST=127.0.0.1

# 数据库位置，默认 server-node/data/mahjong.db
# DB_FILE=/var/lib/ai-red-mahjong/mahjong.db
